pipeline {
    agent any
    stages {
        stage('Trigger Job 1') {
            steps {
                echo 'Starting the first job...'
                // 'job1-name' must match the exact name of your job in Jenkins
                build job: 'job1', wait: true
            }
        }
        stage('Trigger Job 2') {
            steps {
                echo 'Starting the second job...'
                build job: 'job2', wait: true
            }
        }
        stage('Trigger Job 3') {
            steps {
                echo 'Starting the final job...'
                build job: 'job3', wait: true
            }
        }
    }
}
